#include <stdio.h>

int	main(void)
{
	int	*ptr;

	ptr = 0;
    return (0);
}

/**
 *  null: "vacio" 
 * Por convenio si un puntero tiene un valor 0, no apunta a ningún sitio.
 * No tiene dirección real a la que ir.
 * Aplicación: Si tenemos una serie de punteros podemos saber
 * cual va hacia un elemento y cuales no tienen una dirección a la que ir.
*/